# Handoff: Rust Modular Foundation Refactor

Date: 2026-06-30

## Current state

Tasks 0–13 of `docs/plans/2026-06-30-rust-modular-foundation-refactor.md` are implemented. Task 7 passes on the attached Xteink X4 after correcting the migrated absolute-grayscale LUT boundary. Task 8 extracts the complete X4 display policy, Task 9 reduces firmware to platform wiring, Task 10 enforces workspace quality and crate boundaries, Task 11 passes the clean regression matrix, Task 12 passes the final live-device gate, and Task 13 documents the resulting workspace and external-consumer contracts.

The device is running the normal `firmware-bin,diagnostic-console` image on page 3 in grayscale mode. Independent final STATUS reports 16 pages, zero dropped logs, five expected protocol errors generated by the malformed-stream test, and `last_error=0`.

## Implemented foundation

- Unified root Cargo workspace.
- Reusable `binbook-core`, `binbook-decompress`, `gray2-render`, and embedded-hal 1.0 `ssd1677-driver` crates.
- Reusable `xteink-x4-display` owns X4 profile validation, caller-owned streaming buffers, staged/absolute rendering, cancellation, refresh policy, probes, and display-engine state.
- SSD1677 driver accepts `SpiDevice<u8>`, embedded-hal digital pins, sync delay, and async delay without importing `xteink-hal`.
- X4 staged and absolute waveform bytes remain outside the generic driver and are owned by `xteink-x4-display`.
- Firmware `main.rs` is a composition root; focused runtime modules own ADC polling, display adaptation, diagnostic aggregation, and USB diagnostic transport.
- `xteink-hal` and the firmware-local display, panel-driver, refresh, parsing, and PackBits paths are removed.

## Driver-gate defect and fix

The first Task 7 run produced correct corner and clear-white probes but an almost-black `full-refresh-current` image. Old/new SPI trace comparison found that the migrated `GRAY_LUT` contained two extra zero bytes before its tail. That shifted controller policy to gate `0x22`, source `[0x22, 0x17, 0x41]`, and VCOM `0xA8` instead of gate `0x17`, source `[0x41, 0xA8, 0x32]`, and VCOM `0x30`.

The array is restored byte-for-byte. A firmware integration test now locks the 105-byte LUT tail and all voltage bytes. Independent Python-derived fixture offsets and full-plane hashes also lock parser, decompression, and absolute-plane output.

Two additional defects were exposed by the final live gate and are fixed:

- The new `BoardSpiDevice` deasserted chip select without flushing the ESP SPI bus. The first Task 12 image reached the diagnostic console but ended in `panel_mode=Unknown`, `last_error=-1`, and a LOG timeout. `SpiDevice::transaction` now flushes before deselecting, and `board_adapters.rs` locks the ordering as `select, write/read, flush, deselect`.
- The staged-gray CLI used PAGE `last` as a hard-coded page-3 baseline. The 16-page navigation fixture therefore landed on page 15. The exercise now issues PAGE `goto 3`, with a decoded-wire regression test for the exact action and target.

## Host verification

- `cargo test -p ssd1677-driver`: passes.
- `cargo check -p ssd1677-driver --no-default-features --target riscv32imc-unknown-none-elf` with the pinned nightly `rustc`: passes.
- `cargo clippy -p ssd1677-driver --all-targets -- -D warnings`: passes after the LUT fix.
- `cargo test --workspace`: passes after the LUT fix.
- Pinned RISC-V diagnostic release build with `firmware-bin,diagnostic-console`: passes after the LUT fix.
- Task 9 `cargo test -p binbook-fw`, diagnostic feature tests, and workspace tests pass.
- Task 9 pinned RISC-V release sizes:
  - Default: 1,090,836-byte ELF, up 6,400 bytes (0.59%) from the 1,084,436-byte checkpoint.
  - Diagnostic: 1,109,036-byte ELF, up 7,344 bytes (0.67%) from the 1,101,692-byte checkpoint.
  - The bounded growth corresponds to the reusable display engine, semantic event adapter, and caller-owned streaming state; no whole-page or fixed 8 KiB scratch buffer was introduced.
- Workspace Rust warnings and unsafe code are denied through inherited Cargo lints.
- Clippy with `-D warnings` passes for all five reusable crates and diagnostic firmware.
- All five reusable crates pass pinned-nightly no_std checks for `riscv32imc-unknown-none-elf`.
- Cargo metadata/tree inspection confirms these normal dependency boundaries:
  - `binbook-core`: no dependencies.
  - `binbook-decompress`: `binbook-core`, with optional `lz4_flex`.
  - `gray2-render`: no dependencies.
  - `ssd1677-driver`: embedded-hal and embedded-hal-async only.
  - `xteink-x4-display`: the four reusable foundation crates plus embedded-hal traits.
- `Book` construction/indexing, record parsing, and payload I/O are split into focused modules under 130 logical lines each.
- Clean Task 11 regression evidence:
  - `cargo test --workspace`: passes from a fresh `cargo clean` baseline.
  - Diagnostic firmware tests: pass.
  - Serial-device CLI tests: 10 passed and 4 live tests ignored; 11 serial transport tests pass.
  - Python suite: 98 passed, 26 skipped.
  - Explicit kerning-proof suite: exits successfully with all proof tests passing.
  - Pinned release ELF sizes: 1,090,836 bytes default and 1,108,052 bytes diagnostic.
  - Rust CLI help renders the expected command surface.
  - Python encode/inspect/decode round trip validates a 10,512-byte, 18-section fixture; header fields match `BINBOOK_FORMAT_SPEC.md`, and decoded physical pixels exactly match the specified `(799-y, x)` mapping.
- Final Task 13 verification after all source and documentation fixes:
  - `cargo test --workspace`: passes.
  - `cargo test -p binbook-fw --features diagnostic-console`: passes with all feature-gated firmware behavior tests enabled.
  - `cargo test -p binbook-cli --features serial-device`: 10 passed, 4 live tests ignored; all 11 serial transport tests and 35 protocol tests pass.
  - `uv run pytest -q`: 98 passed, 26 skipped.
  - Both flash wrappers pass `bash -n` and resolve the firmware ELF from the root `target/` directory.
  - `git diff --check` passes; stale production-path scanning finds only explicit negative/current-architecture statements and clearly labeled historical plans.

## Live device evidence

- Final flash command: `FW_FEATURES="firmware-bin,diagnostic-console" firmware/scripts/flash-xteink-x4-nav-probe.sh`.
- Final flash output: `/tmp/binbook-rust-refactor-final/flash.txt`.
  - ESP32-C3 revision v0.4, 16 MB flash.
  - Application 1,122,672 bytes.
  - `Flashing has completed!`.
- Final 15-second boot capture: `/tmp/binbook-rust-refactor-final/boot.txt`. It records the ESP-IDF bootloader, 16 MB flash, partition table, all mapped/loaded segments, and `Loaded app from partition at offset 0x10000`.
- HELLO command: `cargo run --features serial-device -- diag hello --port /dev/ttyACM0`.
  - `/tmp/binbook-rust-refactor-final/hello.txt`: `protocol=1 max_frame=512 capabilities=KEY,PAGE,STATUS,LOG,CRASH,DISPLAY_PROBE firmware=binbook-fw target=xteink-x4`.
- Baseline STATUS command: `cargo run --features serial-device -- diag status --port /dev/ttyACM0`.
  - `/tmp/binbook-rust-refactor-final/status-baseline.txt`: `current_page=0 page_count=16 panel_mode=Grayscale dropped_log_count=0 protocol_error_count=0 last_error=0`.
  - `/tmp/binbook-rust-refactor-final/logs-baseline.txt` independently confirms successful startup recovery, waveform 2/LUT revision 1, grayscale overlay, controller-state transitions, and BW base sync.
- Navigation command: `UV_CACHE_DIR=/tmp/binbook-uv-cache uv run --offline python firmware/scripts/run-x4-nav-burst-diagnostic.py --port /dev/ttyACM0 --video-device /dev/video1 --rounds 10 --inter-key-ms 0 --output-dir /tmp/binbook-rust-refactor-final/nav-burst`.
  - `/tmp/binbook-rust-refactor-final/nav-burst/evidence.jsonl` contains 165 KEY results, 165 unique request sequences, 165 sequence-matched `TURN_DEQUEUED` records, boundary results `0,1,0,0,1`, zero error events, and STATUS agreement after every round.
  - `/tmp/binbook-rust-refactor-final/nav-burst/nav-burst.mp4`, 11 contact sheets, per-transition frames, and settled frames show labels matching every modeled target. The boundary contact sheet visibly settles on pages `0,1,0,0,1`.
- Staged-gray command: `cargo run --features serial-device -- diag exercise staged-gray --port /dev/ttyACM0` while `/dev/video1` recorded `/tmp/binbook-rust-refactor-final/staged-gray.mp4`.
  - `/tmp/binbook-rust-refactor-final/staged-gray.txt` records all phases: page-3 baseline 618 ms, page-0 baseline 1,248 ms, prime gray 1,894 ms, delayed refinement 2,938 ms, cancellation window 3,911 ms, batched turns 5,142 ms, base sync complete 6,164 ms, final turn 6,784 ms, final STATUS 6,795 ms, and validated final logs 6,806 ms.
  - `/tmp/binbook-rust-refactor-final/status-after-staged-gray.txt`: `current_page=3 page_count=16 panel_mode=Grayscale dropped_log_count=0 protocol_error_count=0 last_error=0`.
  - `/tmp/binbook-rust-refactor-final/logs-after-staged-gray.txt` independently paginates the full retained range. It records waveform `2`, LUT revision `1`, page-2 overlay cancellation, FIFO dequeues to `2,3,2`, completed base sync for page 2, and final page 3 overlay/base-sync completion without drops or errors.
  - `/tmp/binbook-rust-refactor-final/staged-gray-prime-timeline.jpg` shows the fast BW base followed by delayed dark/light gray separation. `/tmp/binbook-rust-refactor-final/staged-gray-cancellation-timeline.jpg` shows pages `1,2,3,2` without a full-panel white/black flash, inversion, clipping, or incomplete write. `/tmp/binbook-rust-refactor-final/staged-gray-final-page3.jpg` shows the stable final page with four distinct swatches.
- Live transport tests ran sequentially with exact filters. Full outputs are in `transport-byte-by-byte.txt`, `transport-batched.txt`, and `transport-malformed.txt`; each reports one passed test.
  - `/tmp/binbook-rust-refactor-final/status-after-transport.txt`: `current_page=3 page_count=16 panel_mode=Grayscale dropped_log_count=0 protocol_error_count=5 last_error=0`. The increased protocol count is the expected malformed-input effect, and the valid follow-up STATUS proves stream recovery.
- Final native camera command: `ffmpeg -hide_banner -loglevel error -f video4linux2 -i /dev/video1 -frames:v 1 /tmp/binbook-rust-refactor-final/final-settled.jpg`.
  - The 1920x1080 native frame and `final-settled-crop.jpg` show PAGE 03 upright and unclipped, TL triangle, TR circle, BL square, BR diamond, TOP/RIGHT/BOTTOM/LEFT labels, border, center rulers, edge ticks, faint grid, and distinct black/dark-gray/light-gray/white swatches.
- Preserved failure evidence is explicitly named `*-pre-flush.*` and `staged-gray-pre-page-fix.*`. Both failures are resolved by commits `f8b188e` and `c3e54af`; no live-gate failure remains.

## Driver-gate acceptance matrix

| Requirement | Implementation path | Automated test | Observed evidence | State |
|---|---|---|---|---|
| embedded-hal command/reset behavior | `crates/ssd1677-driver` | external command tests | successful init and all probes | Verified |
| window/counter endianness | driver window methods | `tests/windows.rs` | four correct physical corner rectangles | Verified |
| full and partial refresh controls | driver refresh state | `tests/refresh.rs` | clear/corner probes complete | Verified |
| absolute grayscale LUT and voltages | `panel_driver.rs` X4 policy | `grayscale_init_uses_verified_lut_voltage_tail` | PAGE 00 and four swatches restored | Verified |
| parser/decompression/plane stream | core/decompress/render crates | Python-derived offsets and FNV assertions | labeled orientation page matches decoded fixture | Verified |
| protocol state after probes | diagnostic runtime | protocol/engine suites | STATUS: zero protocol/display errors | Verified |

Probe `ok` responses are treated as transport acknowledgements only. Visible outcomes were independently inspected from the fresh webcam files and final state was queried separately.

## Final acceptance matrix

| Requirement | Implementation proof | Automated proof | Serial proof | Webcam proof |
|---|---|---|---|---|
| Five independent reusable crates | Root workspace manifests and Cargo dependency graph | Per-crate tests, Clippy, and pinned no_std checks pass | Not applicable; crate boundaries are compile-time properties | Not applicable; crate boundaries are not visual |
| No firmware-local parsing/decompression/plane/controller logic | Firmware composition/runtime modules import reusable crates; old modules are deleted | Workspace tests and forbidden-pattern scan pass | HELLO and STATUS operate on the thin firmware image | PAGE 03 renders correctly from the reusable pipeline |
| BinBook bytes and parsing remain compatible | Python fixture and `binbook-core` record/index implementation | Python round trip and core golden tests pass | Diagnostic fixture opens as 16 pages | Navigation labels and patterns render for all exercised pages |
| Canonical and staged grayscale remain correct | `gray2-render` planes and `xteink-x4-display` staged engine | Exhaustive byte, plane, staged-state, and cancellation tests pass | Ordered waveform, overlay, base-sync, and controller-state records; final page 3 | Prime/refinement timelines and final frame show four distinct swatches without full flashes |
| SSD1677 command behavior remains correct | embedded-hal driver plus flushed board `SpiDevice` | Exact command/refresh tests and SPI flush-order regression pass | Startup recovery and all render phases succeed with `last_error=0` | Upright, unclipped orientation frame and stable panel output |
| Cancellation and FIFO semantics remain correct | X4 display engine and semantic firmware adapter | Engine/cancellation/boundary tests pass | 165 unique dequeues, boundary `0,1,0,0,1`, staged FIFO `2,3,2`, zero drops | Settled navigation frames and staged timeline match the modeled order |
| RAM discipline is preserved | Caller-owned chunk buffers and streaming APIs | Buffer-boundary tests and both pinned firmware builds pass | No display error or dropped turn under the live burst | No clipping, corruption, or incomplete full-panel writes |
| Firmware behavior is complete | Thin board/runtime adapters and standard HAL traits | Default, diagnostic, CLI serial, Rust workspace, and Python matrices pass | Independent HELLO, STATUS, paginated LOG, staged exercise, and transport tests provide decoded state evidence | Fresh final native capture proves orientation, calibration, and settled page 3 |

## Remaining work

None for the Rust modular foundation refactor. The Xteink X4 remains on the normal diagnostic image at page 3; the five protocol errors in final STATUS are the deliberate malformed-stream test evidence, not an unresolved firmware fault.
